Config = {}

Config.Framework = "NewEsx" -- Changez cette valeur en "esx" ou "qbcore" en fonction de votre framework choisi

Config.Ox = "on" -- On if you use Ox / Off if you don't use Ox !

Config.Item = "bmx" -- Remplacez par l'élément approprié utilisé dans votre script

Config.notificationLibrary = "esxnotifi" -- okok = okokNotify  / vNotif = VCore-Notify / esxnotifi = Notification by default ESX / qbnotify = Notification by default qb-notify

Config.pickupKey = 38 -- Touche E pour récupérer le BMX

Config.TakeAll = false --  [true] peux récupérer les BMX des autres joueurs [false] ne peux pas récupérer le BMX des joueurs
